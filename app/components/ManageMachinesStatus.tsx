import React from "react";
import { useRouter } from "next/navigation";

const ManageMachinesStatus = ({
  id,
  userId,
}: {
  id?: string;
  userId: string;
}) => {
  return <div>{/*todo form + action buttons */}</div>;
};

export default ManageMachinesStatus;
